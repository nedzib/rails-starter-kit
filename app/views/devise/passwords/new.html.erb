<section class="m-auto">
  <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0">
      <%= link_to root_path, class: 'flex items-center mb-6 text-2xl font-semibold text-headline' do %>
          <%= image_tag 'logo', class: "w-8 h-8 mr-2", alt: 'Logo'%>
          <%= I18n.t('project_name') %>
      <% end %>
      <div class="w-full bg-secondary rounded-lg shadow dark:border md:mt-0 sm:max-w-md xl:p-0">
          <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
              <h1 class="text-xl font-bold leading-tight tracking-tight text-gray-900 md:text-2xl dark:text-white">
                  Forgot your password?
              </h1>
              <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
              <div class="field">
                <%= f.label :email, class: "my-2 block text-sm font-medium text-gray-900 dark:text-white"%>
                <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-sky-600 focus:border-sky-600 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500"%>
              </div>

              <div class="actions">
                <%= f.submit "Send me reset password instructions", class: "my-2 w-full text-white font-medium rounded-lg text-sm px-5 py-2.5 text-center text-button-text border-button-text bg-button hover:bg-tertiary" %>
              </div>
            <% end %>
            <span class="text-sm font-light text-gray-500 dark:text-gray-400">
                Want to sign in? <%= link_to "Log in", new_session_path(resource_name), class: "font-medium text-sky-600 hover:underline dark:text-sky-500"  %><br>
            </span>
          </div>
      </div>
  </div>
</section>
